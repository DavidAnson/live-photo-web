<?xml version="1.0"?>
<configuration>
  <system.webServer>
    <staticContent>
      <!-- Some versions of IIS do not include an .mp4 mapping by default -->
      <mimeMap fileExtension=".mp4" mimeType="video/mp4"/>
    </staticContent>
  </system.webServer>
</configuration>
